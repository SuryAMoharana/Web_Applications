server:
  port: 8282

spring:
  application:
    name:SUBMISSION_SERVICES

  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/submission_service_db
    username: root
    password: Surya@1994
    driver-class-name: com.mysql.cj.jdbc.Driver
# spring:
#   jpa:
#     show-sql: true